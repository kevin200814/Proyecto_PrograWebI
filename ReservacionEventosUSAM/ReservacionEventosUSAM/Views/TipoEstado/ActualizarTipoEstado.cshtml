@model ReservacionEventosUSAM.Models.CRUD.TipoEstadoUpdate
@{
    ViewBag.Title = "ActualizarTipoEstado";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ActualizarTipoEstado</h2>

<div class="container">
    <br />
    <div class="card text-center">
        <div class="card-header">
            <ul class="nav nav-tabs card-header-tabs">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="true" href="#">Estado <i class="bi bi-calendar2-event-fill"></i></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Content("~/TipoEstado/ConsultarTipoEstado")">Mostrar Estado <i class="bi bi-list-stars"></i></a>
                </li>
            </ul>
        </div>
        <div class="card-body">
            <h5 class="card-title">Nuevo Estado</h5>

            @using (Html.BeginForm("ActualizarTipoEstado", "TipoEstado", FormMethod.Post, new { role = "form", @class = "" }))
            {

                <div class="row">
                    <div class="col-md-3">
                        <div class="input-group">

                            @Html.HiddenFor(d => d.cod_estado )

                            @Html.LabelFor(d => d.estado, new { @class = "input-group-text" })
                            @Html.TextBoxFor(d => d.estado, new { @class = "form-control" })

                        </div>
                    </div>
                </div>


                <div class="card-footer">
                    <button class="btn btn-primary">GUARDAR <i class="bi bi-sd-card"></i></button>
                    <button class="btn btn-danger">DESCARTAR <i class="bi bi-trash"></i></button>
                </div>
            }
        </div>
    </div>
